%% vvase.sty    For VVASE
\ProvidesPackage{manual}[5/27/2016 for VVASE]

%\raggedbottom

% Common commands
\newcommand{\VVASE}{Virtual Vehicle Analysis and Simulation Environment}
\newcommand{\vvase}{VVASE}
\newcommand{\version}{0.14a}
\newcommand{\sref}[1]{\S~\ref{#1}}
\newcommand{\figref}[1]{Fig.~\ref{#1}}
\newcommand{\link}[2]{\hyperref[#2]{#1}}% create hyperlink, but with user-specified text
\newcommand{\element}{\textit}% TODO:  Better choice here
\newcommand{\button}{\textit}% TODO:  Better choice here
\newcommand{\selection}{\textit}% TODO:  Better choice here

% Set ToC depth
\settocdepth{subsection}
\setsecnumdepth{subsection}

% Set up paragraph defaults after each call to \chapter
\let\oldchapter\chapter
\renewcommand{\chapter}[1]{
	\oldchapter{#1}%\vfil
	\setlength{\parindent}{15pt}
	\setlength{\parskip}{0.2\baselineskip}
}

% Shaded environment for examples
\newenvironment{ex}{%
  \colorlet{shadecolor}{gray!30}
  \begin{shaded}
  \parskip\z@ \topsep\z@%
  \vspace{\onelineskip}
  \small\setlength{\baselineskip}{\onelineskip}\nobreak%
}
{
  \end{shaded}
}

\endinput